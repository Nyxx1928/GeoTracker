[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/laravel-backend/Dockerfile"
buildCommand = ""

[deploy]
startCommand = "/usr/local/bin/start.sh"
healthcheckPath = "/up"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
